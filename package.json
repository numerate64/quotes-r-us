{
  "name": "quotes-r-us",
  "version": "0.2.0",
  "description": "Container-ready shared quote app for ECS Fargate and DynamoDB.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "QUOTE_STORE=local node --watch server.js",
    "check": "node --check server.js && node --check src/quote-store.js && node --check app.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.738.0",
    "@aws-sdk/lib-dynamodb": "^3.738.0",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=22"
  }
}
